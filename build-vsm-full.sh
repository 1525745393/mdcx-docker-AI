#!/bin#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
D#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &>#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

##!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdc#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class V#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION =#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.d#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = byte#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None,#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py;#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py; then
    echo "    VSMETA 已存在，跳过"
else
    ##!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py; then
    echo "    VSMETA 已存在，跳过"
else
    # 在 NFO 后添加 VSMETA
    awk '
        /NFO = "#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py; then
    echo "    VSMETA 已存在，跳过"
else
    # 在 NFO 后添加 VSMETA
    awk '
        /NFO = "nfo"/ {
            print $0;
            print "    VSMETA = \"#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py; then
    echo "    VSMETA 已存在，跳过"
else
    # 在 NFO 后添加 VSMETA
    awk '
        /NFO = "nfo"/ {
            print $0;
            print "    VSMETA = \"vsmeta\"";
            next;
        }
        /DownloadableFile.names()/#!/bin/bash

# 🐳 VSMETA 完整构建脚本
# 在有 Docker 的环境中运行此脚本

set -e

echo "========================================"
echo " 🎬 MDCx VSMETA 完整构建"
echo "========================================"

# 默认配置
DOCKER_HUB_USER="1525745393"
IMAGE_NAME="mdcx-docker-ai"
TAG_PREFIX="vsmeta"

# 检查 Docker
echo "📦 检查 Docker..."
if ! command -v docker &> /dev/null; then
    echo "❌ Docker 未安装！"
    exit 1
fi

if ! command -v docker-compose &> /dev/null && ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose 未安装！"
    exit 1
fi

echo "✅ Docker 已就绪"

# 检查 Docker Hub 登录
echo ""
echo "🔐 检查 Docker Hub 登录..."
if ! docker info 2>/dev/null | grep -q "Username:"; then
    echo "⚠️  未登录到 Docker Hub，是否现在登录？"
    read -p "(y/n): " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        docker login
    fi
fi

# 确认构建
echo ""
echo "========================================"
echo " 构建配置："
echo " Docker Hub 用户: $DOCKER_HUB_USER"
echo " 镜像名称:      $DOCKER_HUB_USER/$IMAGE_NAME"
echo " 标签:          $TAG_PREFIX-latest"
echo "========================================"
read -p "确认开始构建？(y/n): " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "❌ 已取消"
    exit 0
fi

echo ""
echo "========================================"
echo " 📦 步骤 1/4: 准备源码"
echo "========================================"

# 清理旧的源码
if [ -d ".mdcx_src" ]; then
    echo "🗑️  清理旧的源码目录..."
    rm -rf .mdcx_src
fi

# 下载源码
echo "⬇️  下载 MDCx 源码..."
bash prepare-src.sh --context build-mdcx --repo Hazard804/mdcx --tag latest

# 应用 VSMETA 补丁
echo "🔧 应用 VSMETA 补丁..."
cd .mdcx_src

# 创建 VSMETA 编码器
echo "  - 创建 vsmeta.py..."
mkdir -p mdcx/core
cat > mdcx/core/vsmeta.py << 'EOF'
"""Synology Video Station VSMETA encoder"""
import json
import struct
import os
from typing import Dict, Optional

class VSMetaEncoder:
    MAGIC = b"VSMP"
    VERSION = 1
    
    def __init__(self, metadata: Dict):
        self.metadata = metadata
    
    def encode(self, poster_data=None, backdrop_data=None):
        json_data = json.dumps(self.metadata, ensure_ascii=False).encode("utf-8")
        result = bytearray()
        result.extend(self.MAGIC)
        result.extend(struct.pack("<HH", self.VERSION, 0))
        result.extend(struct.pack("<I", len(json_data)))
        result.extend(json_data)
        if poster_data:
            result.extend(struct.pack("<I", len(poster_data)))
            result.extend(poster_data)
        else:
            result.extend(struct.pack("<I", 0))
        if backdrop_data:
            result.extend(struct.pack("<I", len(backdrop_data)))
            result.extend(backdrop_data)
        else:
            result.extend(struct.pack("<I", 0))
        return bytes(result)
    
    def write_file(self, filepath, poster_path=None, backdrop_path=None):
        poster_data = open(poster_path, "rb").read() if poster_path and os.path.exists(poster_path) else None
        backdrop_data = open(backdrop_path, "rb").read() if backdrop_path and os.path.exists(backdrop_path) else None
        with open(filepath, "wb") as f:
            f.write(self.encode(poster_data, backdrop_data))

def write_vsmeta(metadata, output_path, poster_path=None, backdrop_path=None):
    VSMetaEncoder(metadata).write_file(output_path, poster_path, backdrop_path)
EOF

# 修改 enums.py
echo "  - 修改 enums.py..."
if grep -q "VSMETA" mdcx/config/enums.py; then
    echo "    VSMETA 已存在，跳过"
else
    # 在 NFO 后添加 VSMETA
    awk '
        /NFO = "nfo"/ {
            print $0;
            print "    VSMETA = \"vsmeta\"";
            next;
        }
        /DownloadableFile.names()/ {
            in_names = 1;
        }
        in_names && /"N